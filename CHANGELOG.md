# 📝 DJIToGoogleDrive 官方版本更新日志 (Changelog)

所有核心架构变更、功能新增、性能优化与 Bug 修复均完整记录于此。  
本项目版本号严格遵循 [语义化版本 2.0.0 (Semantic Versioning)](https://semver.org/lang/zh-CN/) 规范。

---

## 🛸 [1.2.2] - 2026-09-10

### 🛡️ 修复与系统级治理 (Fixed & Systemic Resilience)
- **macOS 启动台双图标死循环彻底根治（隐身打包）**：
  - 溯源消除了 `package_app.sh` 在桌面源码根目录下残留裸 `.app` 导致 macOS Launch Services 持续重新索引的死循环；
  - 组装流程全面转移至系统级隐藏缓存区 `.build/`，并引入 `lsregister -u` 自动反注册守门员，保证启动台（Launchpad）与聚焦搜索全局永远仅且仅有唯一正式应用入口。
- **界面语言分段器截断彻底修复**：
  - 修复语言切换选择器因定宽挤压导致 `English` 被截断为 `Engli` 的视觉 Bug；
  - 分段器独立另起一行并横向自适应通栏（`maxWidth: .infinity`），两边留白舒展，文字完整展现。

### 🎨 交互与界面优化 (UI & UX Reorganization)
- **偏好设置业务层级全面理顺**：
  - `1. 软件更新与版本` 置于最顶部，方便快速核对版本与升级；
  - 中间高度内聚 Google 云存储设置流：`2. 凭证配置` ➔ `3. 授权状态` ➔ `4. 目标目录与过滤规则`；
  - 低频项 `5. 界面语言` 优雅下沉，信息结构自然清晰。
- **更新亮点按需折叠（Zero Footprint by Default）**：
  - 更新亮点卡片默认收起，顶部首行紧凑呈现 `[✨ 本版更新亮点 ▾]` 轻量胶囊按钮，不占用多余垂直空间；
  - 点击按钮原地微动展开当前版本的核心亮点，再次点击随手收起。
- **界面去噪去重**：
  - 移除窗口内部重复的大标题块，内容整体上移 70pt；
  - 移除标题前多余的旋转小图标，1~5 项统一采用规整纯文字标题排版。

---

## 🛸 [1.2.1] - 2026-09-10

### 🛡️ 修复与稳定性 (Fixed & Resilience)
- **Google Drive 官方 502/5xx 状态探针自愈机制**：
  - 针对十数 GB 巨型全景视频长传因网络代理长连接中断或 Google 边缘服务器抖动导致的 `502 Bad Gateway` 报错，引入 2s+ 指数退避与随机抖动休眠；
  - 自动向 Google 会话发送 `Content-Range: bytes */totalSize` 空探针校验云端接收边界，若分片已入库则自动推进，杜绝盲目重传冲突，支持 5 次后台静默接力自愈。
- **UI 报错文案彻底净化**：
  - 过滤拦截原始 HTML 网页源码，即使极端重试耗尽也仅展示友好中文状态，杜绝红字乱码刷屏。

### ✨ 新增功能 (Added)
- **大疆官方标准学名全域对齐**：
  - 全面废除通俗模糊的“DJI 360 全景相机”等名称，规范对齐大疆官方标准学名：`DJI Osmo 360`、`DJI Osmo Pocket 3`、`DJI Osmo Pocket 4`、`DJI Osmo Action 4/5 Pro`、`DJI Action 2`。
- **硬件芯片级出厂序列号 (SN) 自动感知**：
  - 通过 macOS 原生 IOKit 底层总线遍历匹配 DJI Vendor ID (`11427` / `0x2CA3`)，直接从硬件固件解析出厂序列号（如 `SN: BBBF1E26`），并在设备卡片和素材列表渲染专属序列号徽章。
- **后台静默常驻与即插即感自动提醒**：
  - 依托 `LSUIElement` 实现 24 小时低功耗后台常驻（待机内存约 30MB，CPU 0% 消耗）；
  - 插入大疆硬件 0.1 秒内触发 macOS 原生系统横幅推送与提示音：`🔌 已识别 DJI Osmo 360 [SN: BBBF1E26]`，并自动平滑展开控制面板。
- **App 内置一键自动升级与无感自更**：
  - 告别跳转浏览器下载 zip 的繁琐流程，偏好设置中点击「🚀 一键自动更新并重启」，App 内部流式下载并实时显示百分比；
  - 下载完成后通过系统级 `ditto` 原生解压，后台 0.8 秒无缝物理替换 `/Applications` 并自动重新拉起新版。
- **建立项目级官方《更新日志》总纲**：
  - 新增 `CHANGELOG.md`，并在 App 偏好设置中增设一键查阅入口。

---

## 🌐 [1.2.0] - 2026-09-09

### 🏷️ 品牌与国际化 (Branding & i18n)
- **全局品牌确立为 DJIToGoogleDrive**：
  - 明确 Google Drive 专属生态定位，消除普通云盘歧义。
- **原生代码级轻量中英多语言 (i18n)**：
  - 引入单例中枢 `LocalizationManager`，支持系统语言自适应与偏好设置即时手动切换（跟随系统 / 简体中文 / English），全界面毫秒级无缝重绘，0 外部依赖。

### 🛡️ 存储与清理 (Storage Management)
- **已同步素材卡内物理彻底删除与空间释放**：
  - 在卡片与列表提供单文件与批量彻底删除功能，带二次确认弹窗与释放容量预估；
  - 级联清理相机同名的 `.LRF` 低清代理视频与 `.THM` 缩略图伴随缓存，秒释数十至数百 GB 宝贵卡内存储。
- **多阶梯沉底排序机制 (Multi-tier Sinking Sort)**：
  - 传输中(0) ➔ 排队中(1) ➔ 已暂停(2) ➔ 待同步(3) ➔ 已同步(4，沉底) ➔ 损坏/废片(5，最底)，置顶健康待办。
- **工业级定宽垂直绝对对齐网格**：
  - 消除不同文件名长短导致的操作按钮参差错位；
  - 提供单文件独立暂停/恢复控制，支持排队任务自动顺延接力。

### 🌟 社区与生态 (Ecosystem)
- 深度联动「我有神器 · I Have An App」（[www.ihavean.app](https://www.ihavean.app)）与 Telegram 官方频道（[@ihaveanapp](https://t.me/ihaveanapp)）。

---

## 🎨 [1.1.0] - 2026-09-09

### 🖥️ 视觉与交互优化 (UI & Interaction)
- **界面垂直裁切修复与高度扩容**：
  - 面板高度扩容至 645pt，预留 20pt 顶部安全避让，彻底解决顶部标题被削半与底部栏挤出屏幕的问题。
- **菜单栏图标永久常驻驱动**：
  - 废除断开隐身逻辑，就绪、传输中、完成、断开全域状态机常驻。
- **双卷盘自由切换与专属 AppIcon 装配**：
  - 设备卡片支持机身存储与外置卡下拉切换，装配原生 Retina 多分辨率高清图标。

### ☕ 节能与电源管理 (Power & Sleep)
- **系统级防休眠断言管理 (Sleep Assertion)**：
  - 调用 `beginActivity` 拦截系统睡眠，确保通宵大文件稳定上传；
  - 允许显示屏正常黑屏熄灭，长传不伤屏更节能。

---

## 🌱 [1.0.0] - 2026-09-08

### 🚀 创世首版发布 (Genesis Release)
- **DJI 硬件特征智能识别器 (`DeviceDetector`)**：
  - 支持 Osmo Pocket 3/4、360 全景相机双卷盘自动识别与智能优选。
- **Google Drive 原生 16MB Chunk 断点续传引擎 (`UploadEngine`)**：
  - 专为数十至数百 GB 巨型 4K/8K 视频优化；
  - 配合首尾 4MB 组合哈希秒级对账，实现 0 重复极速秒传。
- **OAuth 2.0 PKCE 鉴权与 Keychain 硬件安全存储 (`AuthManager`)**：
  - Google 官方推荐安全认证标准，Token 物理隔离保存在 macOS Keychain。
- **离线账本引擎 (`Ledger`)**：
  - 本地化 JSON 审计记录，支持换卡、换电脑、跨拔插防重。
